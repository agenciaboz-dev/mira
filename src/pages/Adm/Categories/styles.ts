import colors from "../../../colors"
import { SxProps } from "@mui/material"

interface Styles {
    body: SxProps
}

const styles: Styles = {
    body: {
        width: "100%",
        flexDirection: "column",
    },
}

export default styles
