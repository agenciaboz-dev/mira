import colors from "../../../colors"
import { SxProps } from "@mui/material"

interface Styles {
    body: SxProps
}

const styles: Styles = {
    body: {
        width: "100vw",
    },
}

export default styles
